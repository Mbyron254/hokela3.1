
import { ResetPasswordView } from 'src/auth/view/main';

// ----------------------------------------------------------------------

export const metadata = { title: `Reset password | Amplify - Hokela 3.1` };

export default function Page() {
  return <ResetPasswordView />;
}
